*This repository is a mirror of the [component](http://component.io) module [segmentio/is-url](http://github.com/segmentio/is-url). It has been modified to work with NPM+Browserify. You can install it using the command `npm install npmcomponent/segmentio-is-url`. Please do not open issues or send pull requests against this repo. If you have issues with this repo, report it to [npmcomponent](https://github.com/airportyh/npmcomponent).*
# is-url

  Check whether a string is a URL.

## Installation

```
$ component install segmentio/is-url
```
```
$ npm install is-url
```

## API

### isUrl(string)

  Checks whether `string` is a URL.

## License

  MIT
